2024-03-06

foreach는 동적쿼리할 때 좋다
update를 이용하여 +1을 해줬는데 , 다시 -1 하는 부분에 대해 생각해봐야함-> 프로젝트


응답할 때는 spring을 반환했다 이번엔 map이다
서비스 그대로 반환해도 된다
controller.java
requestMapping postMapping getMapping 이중에 하고싶은거 하면된다
recomment

그냥 반환해버리면 끝이다

직렬화 -> responseBody 직렬화 하는 것이다
RestController 하면 자동적으로 직렬화 한다 -> 이건 금요일에 말할거다

직렬화로 // map이니까 key value형태로 반환될 것
-> 객체화 시킬 수 있다

j쿼리가 객체로 변화시켜 줄 것이다 저 형태가 안맞으면 오류가 날 것


ajax를 사용해서 통신을 하는건가?

천천히 잡아줘야 함
display none으로 잡아주기
css를 통해서 스타일 맞춰준다

안보이는 상태에서 보이는 상태로 천천히 다운이 될 것이다

-> 다른 곳에서 수정누르면 폼 안에 있는 것을 가지고 와야한다.

js 파일에서 slideup과 down을 설정해준다.
아이디를 넣고 그 아이디를 접을 건지 펼쳐줄 건지 설정해주는 것

접은 다음에접히는 시간만큼 대기했다가 접히는 것이다


-> 댓글 수정하기는 내일 오늘 댓글쓰기까지 진행했다

댓글쓰기까지 했는데 클릭되었을 때 서블릿 버튼 !!!!


성공하고 실패했을 때에 대한 콜백 설정하기
success: function()
errir:function()

데이터 서버로 보내야 한다

댓글 리스트 하나 추가된 것
전체 읽어와서 한번에 뿌려주는 것으로 한다
지우고 새로운 것을 채워줘야 한다

읽어오는 건 이미 만들어져 있다
이 댓글 뿌릴 때 만들었다 그거 사용하면 된다
DB에 넣어주기-> 만들어주기

개수만큼 댓글이 추가될 것이다
result에 담아야 최종적으로 쓸 수 있다